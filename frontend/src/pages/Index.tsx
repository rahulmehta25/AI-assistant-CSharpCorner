// This page is now replaced by the Dashboard component
// The new routing structure uses a Layout component with nested routes

export default function Index() {
  // This component is no longer used as the main entry point
  return null;
}
